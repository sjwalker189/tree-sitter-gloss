enum Bool {
	True,
	False,
}

enum Unit {
	Off,
	On = 1,
}

enum State {
	On = "on",
	Off = "off",
}


union Option<T> {
	Some(T),
	None,
}

struct User<T> {
 name: string,
 age: int,
 meta: T,
}

fn newUser(name string) User {
	return User<bool>{
		name: "Sam",
		age: 31,
		meta: true,
 	}
}

let enabled = true

fn main() bool {
 let user = newUser("sam")

 newUser(:name, :name)


}
